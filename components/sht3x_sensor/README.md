# SHT3x Sensor (Legacy)

This component supports the older Sensirion SHT3x series. **ThermoFlow firmware uses SHT4x** via `sensor_manager` + `sht4x_sensor`.

Kept for reference and potential hardware migration. Not integrated in `main.c`.